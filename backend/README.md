# Backend

FastAPI backend skeleton for Milestone 1.

## Run locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Health endpoint: `http://localhost:8000/api/v1/health`

