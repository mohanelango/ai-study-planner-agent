# Frontend

React + Vite app for creating study plans and reviewing generated sessions.

## Local setup

```bash
npm install
npm run dev
```

The app expects the backend at `http://localhost:8000/api/v1`.
Override it with `VITE_API_BASE_URL`.
