# MinIO

MinIO stores uploaded study materials before parsing and embedding.

The docker compose file creates a local bucket service on:

- Console: `http://localhost:9001`
- API: `http://localhost:9000`
